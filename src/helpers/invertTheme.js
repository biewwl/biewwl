export default function invert(theme) {
  if (theme === '') return '-dark';
  return '';
}

// Todas as cores são convertidas em preto, exceto a cor -dark que é convertida para branco, o invert, inverte entre dark e light;
